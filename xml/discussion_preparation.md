# 🎓 شرح المشاريع والربط بالمحاضرة

## الفكرة العامة: ليه بندرس XML و JSON؟

تخيل إنك عندك برنامج Java وعايز:

1. **تحفظ إعدادات** (مثلاً: اسم المستخدم، اللغة، الثيم)
2. **تتواصل مع API** (مثلاً: تجيب بيانات الطقس من الإنترنت)
3. **تبادل بيانات** بين برنامجين مختلفين

**المشكلة**: Java Objects موجودة في الـ RAM بس، لما البرنامج يقفل بتضيع!

**الحل**: نحول الـ Objects لـ نص (Text) نقدر نحفظه أو نبعته:

- **XML**: صيغة قديمة ومنظمة (زي HTML)
- **JSON**: صيغة حديثة وخفيفة (زي JavaScript Objects)

---

# 📁 المشروع الأول: Library XML

## الملف: `library.xml`

### 🎯 الهدف من المشروع:

تصميم ملف Configuration (إعدادات) لمكتبة باستخدام XML.

### 🔗 علاقته بالمحاضرة:

الدكتور طلب منكم تفهموا **عناصر XML الأساسية**:

| العنصر في المحاضرة | مثال من المشروع             | الشرح                                 |
| ------------------ | --------------------------- | ------------------------------------- |
| **Element عادي**   | `<title>Java Book</title>`  | عنصر فيه محتوى                        |
| **Empty Element**  | `<status open="true"/>`     | عنصر فاضي - مفيهوش محتوى بين الـ tags |
| **Attribute**      | `<book isbn="978-123">`     | خاصية جوه الـ tag نفسه                |
| **CDATA**          | `<![CDATA[text & <code>]]>` | نص حرفي فيه رموز خاصة                 |

### 📝 لو الدكتور سألك:

**س: إيه الفرق بين Element و Attribute؟**

> Element = بيانات رئيسية (العنوان، المؤلف)
> Attribute = metadata أو خصائص إضافية (الـ ID، اللغة)

**س: امتى نستخدم CDATA؟**

> لما يكون عندنا نص فيه رموز خاصة زي `<` أو `>` أو `&`

**س: امتى نستخدم Empty Element؟**

> لما العنصر مفيهوش محتوى، بس فيه attributes
> مثال: `<image src="photo.jpg"/>` أو `<br/>`

---

# 📁 المشروع الثاني: JSON Demos (Java)

## الفكرة العامة:

3 طرق للتعامل مع JSON في Java:

| الطريقة             | الوصف                      |
| ------------------- | -------------------------- |
| JSON-P (JsonReader) | قراءة JSON يدوياً          |
| JSON-P (JsonWriter) | كتابة JSON يدوياً          |
| JSON-B (Binding)    | تحويل تلقائي Object ↔ JSON |

---

## Demo 1: JsonReader (قراءة JSON)

### 🎯 الهدف:

عندك ملف JSON وعايز **تقرأ** البيانات منه.

### الخطوات:

```java
// 1️⃣ عندك JSON String
String json = "{\"name\":\"أحمد\", \"age\":25}";

// 2️⃣ تعمل JsonReader
JsonReader reader = Json.createReader(new StringReader(json));

// 3️⃣ تقرأ الـ JSON كـ Object
JsonObject person = reader.readObject();

// 4️⃣ تستخرج البيانات بـ getXXX()
String name = person.getString("name");  // "أحمد"
int age = person.getInt("age");          // 25
```

### 📝 لو الدكتور سألك:

**س: إيه الـ methods اللي بنستخدمها لاستخراج البيانات؟**

> - `getString("key")` → للنصوص
> - `getInt("key")` → للأرقام الصحيحة
> - `getJsonArray("key")` → للـ Arrays
> - `getJsonObject("key")` → للـ Nested Objects

---

## Demo 2: JsonWriter (كتابة JSON)

### 🎯 الهدف:

عندك بيانات وعايز **تكتبها** كـ JSON.

### الخطوات:

```java
// 1️⃣ تبني الـ JSON باستخدام Builders
JsonObject person = Json.createObjectBuilder()
    .add("name", "سارة")
    .add("age", 22)
    .add("skills", Json.createArrayBuilder()
        .add("Java")
        .add("Python"))
    .build();

// 2️⃣ تعمل JsonWriter
JsonWriter writer = Json.createWriter(new FileWriter("output.json"));

// 3️⃣ تكتب الـ Object
writer.writeObject(person);
writer.close();
```

### 📝 لو الدكتور سألك:

**س: إيه الـ Builders اللي بنستخدمها؟**

> - `Json.createObjectBuilder()` → لعمل `{ }`
> - `Json.createArrayBuilder()` → لعمل `[ ]`

**س: ليه بنعمل `.build()` في الآخر؟**

> عشان نحول الـ Builder لـ JsonObject نهائي (immutable)

---

## Demo 3: JSON-B (Binding)

### 🎯 الهدف:

تحويل **تلقائي** بين Java Objects و JSON.

### الخطوات:

```java
// عندك Class عادي
public class Dog {
    public String name;
    public int age;
    public boolean bitable;
}

// 1️⃣ إنشاء Jsonb
Jsonb jsonb = JsonbBuilder.create();

// 2️⃣ Serialize (Object → JSON)
Dog dog = new Dog("Falco", 4, false);
String json = jsonb.toJson(dog);
// النتيجة: {"age":4,"bitable":false,"name":"Falco"}

// 3️⃣ Deserialize (JSON → Object)
Dog sameDog = jsonb.fromJson(json, Dog.class);
```

### 📝 لو الدكتور سألك:

**س: إيه الفرق بين JSON-P و JSON-B؟**

> - **JSON-P**: تتحكم في كل حاجة يدوي (Low-level)
> - **JSON-B**: تحويل تلقائي بناءً على الـ Class (High-level)

**س: إيه متطلبات الـ Class عشان JSON-B يشتغل؟**

> - **Default Constructor** (constructor فاضي)
> - **Public fields** أو **Getters/Setters**

---

# 🎯 ملخص للمناقشة

| الموضوع    | الخلاصة                       |
| ---------- | ----------------------------- |
| **XML**    | Elements + Attributes + CDATA |
| **JSON-P** | قراءة/كتابة يدوي (Low-level)  |
| **JSON-B** | تحويل تلقائي (High-level)     |

---

# 💡 نصائح للمناقشة

1. **لو سألك عن XML**: ركز على الفرق بين Element و Attribute و CDATA
2. **لو سألك عن JSON-P**: اشرح إنها Low-level API وبتتحكم في كل حاجة
3. **لو سألك عن JSON-B**: اشرح إنها Binding - تحويل تلقائي
4. **لو سألك عن الفرق**: JSON-P = يدوي، JSON-B = تلقائي

**بالتوفيق! 🚀**
