# 📘 دليل المبتدئين: JSON في Java من الصفر (خطوة بخطوة)

> **ملاحظة مهمة:** هذا الملف مصمم للمبتدئين تماماً. كل سطر كود مشروح بالتفصيل.

---

# 🎯 الفهرس

1. **المقدمة النظرية** - ليه محتاجين JSON في Java؟
2. **فهم الـ Imports** - كل import ليه موجود؟
3. **الأساسيات: قراءة JSON بسيطة جداً**
4. **فهم JsonReader Class** - إيه ده؟ إزاي يشتغل؟
5. **فهم JsonObject Class** - إيه ده؟ إيه الـ Methods؟
6. **كتابة JSON من الصفر**
7. **فهم JsonWriter و Builders**
8. **أمثلة عملية متدرجة**

---

# 📚 الجزء 1: المقدمة النظرية (بدون كود)

## السؤال الأول: ليه أصلاً محتاجين JSON في Java؟

تخيل معايا الموقف ده:

### الموقف:

أنت بتعمل برنامج Java، والبرنامج ده محتاج يتواصل مع:

1. **موقع ويب** (مكتوب بـ JavaScript)
2. **تطبيق موبايل** (مكتوب بـ Kotlin)
3. **سيرفر تاني** (مكتوب بـ Python)

### المشكلة:

كل لغة بتمثل البيانات بطريقة مختلفة:

- Java: `Student student = new Student("أحمد", 22)`
- JavaScript: `let student = {name: "أحمد", age: 22}`
- Python: `student = {"name": "أحمد", "age": 22}`

**إزاي يتفاهموا؟** 🤔

### الحل: JSON (اللغة المشتركة)

JSON هو **نص عادي** (Plain Text) كل اللغات تعرف تقراه وتكتبه:

```json
{
  "name": "أحمد",
  "age": 22
}
```

**الفكرة:**

```
برنامج Java  →  تحويل Object لـ JSON  →  إرسال النص
برنامج آخر  ←  قراءة JSON  ←  تحويل JSON لـ Object في لغته
```

---

## السؤال الثاني: طيب Java إزاي تتعامل مع JSON؟

Java مش بتفهم JSON لوحدها! محتاجين **مكتبة** (library) تساعدنا.

### المكتبات المتاحة:

| المكتبة     | الوصف                                     | مناسبة لـ        |
| ----------- | ----------------------------------------- | ---------------- |
| **JSON-P**  | مكتبة Java الرسمية (Part of Jakarta EE)   | التحكم الكامل    |
| **JSON-B**  | مكتبة Java الرسمية (Binding - ربط تلقائي) | السرعة والسهولة  |
| **Gson**    | مكتبة Google                              | المشاريع البسيطة |
| **Jackson** | الأشهر في المشاريع الكبيرة                | Enterprise       |

**في المحاضرة هنركز على JSON-P و JSON-B** (الرسميين من Java).

---

# 📦 الجزء 2: فهم الـ Imports (قبل ما نكتب كود!)

## إيه هو الـ Import أصلاً؟

الـ `import` هو **استيراد**. بنقول لـ Java: "يا Java، أنا محتاج أستخدم Class معين، جيبهولي".

### مثال من الحياة العادية:

```
أنت في البيت (برنامجك)
محتاج مفك (Class)
المفك موجود في صندوق الأدوات (Package/Library)

import = "روح جيب المفك من الصندوق"
```

---

## الـ Imports اللي هنستخدمها:

### 1. `import jakarta.json.*;`

```java
import jakarta.json.*;
```

**الشرح:**

- `jakarta.json` = اسم الـ Package (الصندوق)
- `*` = جيب **كل** اللي جوه الصندوق ده

**إيه اللي جوه `jakarta.json`؟**

- `Json` = Class رئيسي (المصنع)
- `JsonReader` = Class للقراءة
- `JsonWriter` = Class للكتابة
- `JsonObject` = Class يمثل `{ }` في JSON
- `JsonArray` = Class يمثل `[ ]` في JSON

**ليه `jakarta` مش `javax`؟**

- قديماً كانت `javax.json` (Java EE)
- دلوقتي بقت `jakarta.json` (Jakarta EE)
- نفس الحاجة، بس الاسم اتغير

---

### 2. `import java.io.*;`

```java
import java.io.*;
```

**الشرح:**

- `java.io` = Package للتعامل مع Input/Output (القراءة والكتابة)

**إيه اللي جوه `java.io`؟**

- `StringReader` = قراءة من String
- `FileReader` = قراءة من File
- `FileWriter` = كتابة لـ File
- `InputStream` = قراءة من Stream

**متى نحتاجه؟**

- لما نقرأ JSON من String أو File
- لما نكتب JSON لـ File

---

### 3. `import java.util.*;`

```java
import java.util.*;
```

**الشرح:**

- `java.util` = Package للأدوات المساعدة (Utilities)

**إيه اللي جوه؟**

- `List` = قائمة
- `ArrayList` = تطبيق للقائمة
- `Map` = خريطة (key-value)

**متى نحتاجه؟**

- لما نتعامل مع Arrays أو Collections في JSON

---

# 🔰 الجزء 3: قراءة JSON - بساطة جداً

## المثال الأبسط على الإطلاق:

### 📝 الهدف:

عندنا JSON بسيط فيه اسم وعمر، عايزين **نقراه** في Java.

### الـ JSON:

```json
{
  "name": "أحمد",
  "age": 25
}
```

### الفكرة (بدون كود):

```
1. خزنا الـ JSON في String (نص)
2. عملنا "قارئ" (JsonReader)
3. قلنا للقارئ: "اقرأ الكلام ده"
4. استخرجنا الاسم والعمر
```

---

## الكود الكامل (مع شرح كل سطر):

```java
import jakarta.json.*;      // المكتبة الأساسية
import java.io.*;           // للتعامل مع String/File

public class FirstJsonExample {
    public static void main(String[] args) {

        // ========== الخطوة 1: تحضير الـ JSON ==========
        String jsonText = "{\"name\":\"أحمد\", \"age\":25}";
        // لاحظ: استخدمنا \" عشان نكتب " جوه String

        // ========== الخطوة 2: إنشاء "القارئ" ==========
        // StringReader = قارئ نصوص (بياخد String ويحوله لـ Stream)
        StringReader stringReader = new StringReader(jsonText);

        // Json.createReader = مصنع بيصنع JsonReader
        // بناخد: stringReader (مصدر النص)
        // بنرجع: JsonReader (القارئ الجاهز)
        JsonReader jsonReader = Json.createReader(stringReader);

        // ========== الخطوة 3: القراءة ==========
        // readObject() = اقرأ JSON كـ Object
        // بترجع: JsonObject (كائن يمثل البيانات)
        JsonObject person = jsonReader.readObject();

        // ========== الخطوة 4: إغلاق القارئ (مهم!) ==========
        jsonReader.close();
        // زي ما بنقفل الكتاب بعد ما نقراه

        // ========== الخطوة 5: استخراج البيانات ==========
        // getString("key") = جيب النص المخزن في المفتاح ده
        // بتاخد: String (اسم المفتاح)
        // بترجع: String (القيمة)
        String name = person.getString("name");

        // getInt("key") = جيب الرقم الصحيح المخزن في المفتاح ده
        // بتاخد: String (اسم المفتاح)
        // بترجع: int (القيمة)
        int age = person.getInt("age");

        // ========== الخطوة 6: الطباعة ==========
        System.out.println("الاسم: " + name);
        System.out.println("العمر: " + age);
    }
}
```

### Output (النتيجة):

```
الاسم: أحمد
العمر: 25
```

---

## شرح تفصيلي لأهم النقاط:

### 1. `StringReader stringReader = new StringReader(jsonText);`

**السؤال:** ليه محتاجين StringReader؟ ليه مش نديله الـ String مباشرة؟

**الإجابة:**

- الـ `JsonReader` مش بياخد String مباشرة.
- هو بياخد "مجرى" (Stream) من البيانات.
- الـ `StringReader` بيحول الـ String لـ Stream.

**تشبيه:**

```
الـ JSON = زجاجة مياه
StringReader = خرطوم
JsonReader = الشخص اللي بيشرب

JsonReader مش بيعرف ياخد من الزجاجة مباشرة، لازم خرطوم!
```

---

### 2. `JsonReader jsonReader = Json.createReader(stringReader);`

**السؤال:** إيه هو `Json.createReader`؟

**الإجابة:**

- `Json` = Class رئيسي (زي المصنع)
- `createReader` = Method جواه (زي ماكينة في المصنع)
- بتصنع `JsonReader` جديد

**Return Type:** `JsonReader`

**Parameters (المدخلات):**

- بتاخد `Reader` (مصدر البيانات)
- ممكن `StringReader`, `FileReader`, `InputStreamReader`, إلخ.

---

### 3. `JsonObject person = jsonReader.readObject();`

**السؤال:** إيه هو `JsonObject`؟

**الإجابة:**

- `JsonObject` = Class بيمثل `{ }` في JSON
- زي `Map` (خريطة) - كل key ليها value

**Return Type:** `JsonObject`

**إمتى نستخدم `readObject()`؟**

- لما الـ JSON بيبدأ بـ `{`

**في Methods تانية؟**

- `readArray()` = لما الـ JSON بيبدأ بـ `[`
- `read()` = أي نوع

---

### 4. `String name = person.getString("name");`

**السؤال:** إيه الـ Methods المتاحة في `JsonObject`؟

**الإجابة:**

| Method                 | Return Type  | متى نستخدمها           | مثال                |
| ---------------------- | ------------ | ---------------------- | ------------------- |
| `getString("key")`     | `String`     | لما القيمة نص          | `"أحمد"`            |
| `getInt("key")`        | `int`        | لما القيمة رقم صحيح    | `25`                |
| `getBoolean("key")`    | `boolean`    | لما القيمة true/false  | `true`              |
| `getJsonObject("key")` | `JsonObject` | لما القيمة `{ }`       | `{"city": "Cairo"}` |
| `getJsonArray("key")`  | `JsonArray`  | لما القيمة `[ ]`       | `[1, 2, 3]`         |
| `isNull("key")`        | `boolean`    | نتأكد لو القيمة `null` | `null`              |

---

# 🏋️ تمرين عملي 1 (ابدأ بسيط)

## المطلوب:

عندك الـ JSON ده:

```json
{
  "city": "القاهرة",
  "population": 10000000,
  "isCapital": true
}
```

**اكتب كود يقرأ الـ JSON ده ويطبع:**

```
المدينة: القاهرة
عدد السكان: 10000000
هل هي عاصمة؟ true
```

<details>
<summary>💡 الحل (اضغط هنا)</summary>

```java
import jakarta.json.*;
import java.io.*;

public class CityExample {
    public static void main(String[] args) {
        // 1. الـ JSON
        String json = "{\"city\":\"القاهرة\", \"population\":10000000, \"isCapital\":true}";

        // 2. إنشاء القارئ
        JsonReader reader = Json.createReader(new StringReader(json));

        // 3. القراءة
        JsonObject cityData = reader.readObject();
        reader.close();

        // 4. الاستخراج
        String city = cityData.getString("city");
        int population = cityData.getInt("population");
        boolean isCapital = cityData.getBoolean("isCapital");

        // 5. الطباعة
        System.out.println("المدينة: " + city);
        System.out.println("عدد السكان: " + population);
        System.out.println("هل هي عاصمة؟ " + isCapital);
    }
}
```

</details>

---

# ✍️ الجزء 4: كتابة JSON من الصفر

## المثال الأبسط: كتابة JSON بسيط

### 📝 الهدف:

نعمل JSON فيه اسم وعمر **من الصفر**.

### الفكرة (بدون كود):

```
1. نستخدم "البنّاء" (JsonObjectBuilder)
2. نضيف البيانات واحدة واحدة
3. نبني (build) الـ Object النهائي
4. نكتبه
```

---

## الكود الكامل (مع شرح كل سطر):

```java
import jakarta.json.*;
import java.io.*;

public class WriteJsonExample {
    public static void main(String[] args) {

        // ========== الخطوة 1: البناء ==========
        // Json.createObjectBuilder() = مصنع بيصنع "بنّاء" جديد
        // بترجع: JsonObjectBuilder (الأداة اللي بنبني بيها)
        JsonObjectBuilder builder = Json.createObjectBuilder();

        // ========== الخطوة 2: إضافة البيانات ==========
        // add(key, value) = ضيف key-value pair
        // بتاخد: String (المفتاح), Object (القيمة)
        // بترجع: JsonObjectBuilder (نفس الـ builder عشان نقدر نربط)
        builder.add("name", "سارة");
        builder.add("age", 22);
        builder.add("isStudent", true);

        // ========== الخطوة 3: البناء النهائي ==========
        // build() = اصنع الـ JsonObject النهائي
        // بترجع: JsonObject (الكائن الجاهز)
        JsonObject person = builder.build();

        // ========== الخطوة 4: الكتابة ==========
        // StringWriter = كاتب نصوص (بيكتب في String)
        StringWriter stringWriter = new StringWriter();

        // Json.createWriter = مصنع بيصنع JsonWriter
        // بتاخد: Writer (وجهة الكتابة)
        // بترجع: JsonWriter (الكاتب الجاهز)
        JsonWriter writer = Json.createWriter(stringWriter);

        // writeObject = اكتب الـ JsonObject
        // بتاخد: JsonObject
        // بترجع: void (مبترجعش حاجة)
        writer.writeObject(person);
        writer.close();

        // ========== الخطوة 5: الطباعة ==========
        String result = stringWriter.toString();
        System.out.println(result);
    }
}
```

### Output:

```json
{ "name": "سارة", "age": 22, "isStudent": true }
```

---

## شرح المفاهيم المهمة:

### 1. JsonObjectBuilder - البنّاء

**السؤال:** ليه مش نعمل JsonObject مباشرة؟

**الإجابة:**

- الـ `JsonObject` **immutable** (مينفعش نغيره بعد ما نعمله)
- الـ `JsonObjectBuilder` **mutable** (نقدر نضيف ونعدل)
- بنبني باستخدام الـ Builder، وفي الآخر نعمل `build()` عشان نحصل على الـ Object النهائي

**تشبيه:**

```
JsonObjectBuilder = مهندس بيبني بيت
JsonObject = البيت الخلص (مينفعش نغيره)

بنبني كله، وفي الآخر نسلم البيت
```

---

### 2. Method Chaining (الربط)

**ممكن نكتب الكود ده:**

```java
builder.add("name", "سارة");
builder.add("age", 22);
```

**بطريقة أقصر:**

```java
builder.add("name", "سارة")
       .add("age", 22);
```

**ليه؟** لأن `add()` بترجع الـ `builder` نفسه، فنقدر نربط!

**الطريقة الأقصر (كل حاجة في سطر واحد):**

```java
JsonObject person = Json.createObjectBuilder()
    .add("name", "سارة")
    .add("age", 22)
    .add("isStudent", true)
    .build();
```

---

# 🏋️ تمرين عملي 2

## المطلوب:

اكتب كود ينشئ JSON ده:

```json
{
  "productName": "لابتوب",
  "price": 15000,
  "inStock": true
}
```

<details>
<summary>💡 الحل</summary>

```java
import jakarta.json.*;
import java.io.*;

public class ProductExample {
    public static void main(String[] args) {
        // البناء
        JsonObject product = Json.createObjectBuilder()
            .add("productName", "لابتوب")
            .add("price", 15000)
            .add("inStock", true)
            .build();

        // الكتابة
        StringWriter sw = new StringWriter();
        JsonWriter writer = Json.createWriter(sw);
        writer.writeObject(product);
        writer.close();

        // الطباعة
        System.out.println(sw.toString());
    }
}
```

</details>

---

**يتبع في الملف التالي...** (هكمل باقي الأجزاء في ملفات منفصلة عشان ميبقاش طويل أوي)

**بالتوفيق! 🚀**
