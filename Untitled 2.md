**  

### 1. ما هو الـ Lexical Environment (LE)؟

ببساطة، هو Data Structure داخلية المحرك بيعملها عشان يخزن فيها المتغيرات والفانكشنز اللي متعرفة في "مكان" معين في الكود. كلمة Lexical معناها "مكاني" أو "ترتيبي"؛ يعني LE بيتحدد بناءً على أنت كاتب الكود فين بالظبط.

بيتكون من جزئين (التفاصيل اللي بيحبوها في الـ ITI):

1. Environment Record: ده الجدول الحقيقي اللي فيه أسماء المتغيرات وقيمها (زي: x: 10, user: {name: 'Khaled'}).
    
2. Outer Lexical Environment Reference: ده "السلك" اللي بيوصل الـ LE ده بالـ LE اللي فوقيه (الـ Parent). ده هو المسؤول عن عمل الـ Scope Chain.
    

---

### 2. إيه الفرق بينه وبين الـ Variable Environment (VE)؟

دي حتة "دسمة" جداً.. في بدايات الجافا سكريبت كان فيه بيئة واحدة، لكن مع ظهور ES6 (let و const)، المحرك بقى بيقسم البيئات جوه الـ Execution Context لـ نوعين:

- Variable Environment (VE): ده "المخزن القديم"، وده مخصص فقط للمتغيرات اللي متعرفة بـ var.
    
- Lexical Environment (LE): ده "المخزن الحديث"، وده مخصص للمتغيرات اللي متعرفة بـ let و const و الـ class.
    

ليه فصلوهم؟ عشان يقدروا يطبقوا قوانين الـ Block Scope. الـ var ملوش دعوة بالـ {} (Block)، لكن الـ let والـ const بيحترموا الـ Block. فكل ما بتفتح {}، المحرك بيعمل Lexical Environment جديد خاص بالـ Block ده بس، ويسيب الـ Variable Environment زي ما هو للـ Function كلها.

---

### 3. الربط بالـ Execution Context (غرفة العمليات)

تخيل الـ Execution Context هو "الحاوية" (Container) الكبيرة اللي بتشغل الكود. جوه الحاوية دي فيه:

1. الـ Lexical Environment (للمتغيرات الحديثة والـ Block scope).
    
2. الـ Variable Environment (للمتغيرات الـ var).
    
3. قيمة الـ this (اللي بتتحدد لكسيكالياً في الـ Arrow functions).
    

---

### 4. الـ Lexical Environment والـ Closures (الذاكرة الممتدة)

ده السر اللي بيخلي الـ Closure يشتغل. لما الفانكشن بتخلص تنفيذ، الـ Execution Context بتاعها بيتمسح، لكن لو فيه فانكشن داخلية لسه "قفشة" في متغيراتها، الـ Lexical Environment بيفضل موجود في الميموري مش بيتمسح. الفانكشن الداخلية بتفضل محتفظة بـ Outer Reference بيشاور على الـ LE القديم ده، وده هو الـ Closure.

بما أننا "فرتكنا" الـ Lexical Environment، فاحنا دلوقتي محتاجين نتكلم عن "الحاوية" (Container) اللي بتشيل الـ Environment ده وكل أخواته. النقطة الثانية هي الـ Execution Context (سياق التنفيذ).

لو الـ Lexical Environment هو "القاموس" اللي فيه الكلمات، فالـ Execution Context هو "غرفة العمليات" اللي الكود بيتحلّل ويتنفذ فيها فعلياً.

---

### 2. الـ Execution Context (غرفة العمليات المركزية)

في جافا سكريبت، مفيش سطر كود واحد بيشتغل "في الهوا". أي سطر بيتنفذ لازم يكون جوه Execution Context (EC). ده مفهوم "تجريدي" (Abstract) بيوصف البيئة اللي المحرك بيجهزها عشان يشغل الكود الحالي.

#### أ. أنواع الـ Execution Context

المحرك بيخلق 3 أنواع من الغرف دي:

1. Global Execution Context (GEC): دي الغرفة "الأم". بتتخلق أول ما تشغل الملف. بتبقى واحدة بس طول فترة حياة البرنامج، وهي اللي بتعمل الـ window object (في المتصفح) أو الـ global (في نود) وبتربط كلمة this بيهم.
    
2. Function Execution Context (FEC): كل ما "تنادي" فانكشن، المحرك بيفتح لها غرفة عمليات خاصة بيها فوراً. لو ناديت الفانكشن 10 مرات، هيفتح لها 10 غرف (Contexts) منفصلة.
    
3. Eval Execution Context: (نادر الاستخدام) وده لما بتستخدم eval() لشغل كود مكتوب كـ String.
    

---

#### ب. مكونات الـ Execution Context (إيه اللي جوه الصندوق؟)

لما المحرك يفتح EC لفانكشن معينة، بيحط جواه 3 حاجات أساسية:

1. Lexical Environment (LE): (اللي شرحناه بالتفصيل) وده اللي شايل الـ let, const, والـ functions والـ Outer Reference.
    
2. Variable Environment (VE): وده "الركن القديم" اللي شايل الـ var والـ arguments object بتاع الفانكشن.
    
3. This Binding: ده مكان بيتحدد فيه قيمة كلمة this في الـ Context ده. (في الـ Arrow functions المكان ده بيبقى فاضي وبيروح يجيب القيمة من الـ LE الخارجي).
    

---

#### ج. مراحل حياة الـ Execution Context (نظام السنتين)

المحرك مش "بيخبط" في الكود مرة واحدة؛ هو بيمشي بنظام المرحلتين:

1. مرحلة الإنشاء (Creation Phase):

قبل ما ينفذ أول سطر كود، المحرك بيعمل الآتي:

- بيخلق الـ Lexical Environment والـ Variable Environment.
    
- بيعمل Hoisting: يروح يدور على كل الـ function declarations ويحطها كاملة في الميموري، ويدور على الـ var ويديهم undefined.
    
- بيحدد الـ Outer Reference (بيشوف الفانكشن دي "اتكتبت" فين عشان يوصلها باللي فوقيها).
    
- بيحدد قيمة الـ this.
    

2. مرحلة التنفيذ (Execution Phase):

هنا المحرك بيبدأ يمشي على الكود سطر بسطر (Line-by-line):

- بيعمل Assign للقيم الحقيقية للمتغيرات (يعني الـ x اللي كانت undefined تاخد قيمتها اللي أنت كاتبها).
    
- بيشغل الـ Logic (حسابات، if statements، نداء لفانكشنز تانية).
    

---

#### د. الـ Call Stack (منظم المرور)

بما إن الجافا سكريبت Single-threaded (يعني عندها إيد واحدة بس بتشتغل)، فإزاي بتعرف هي في أنهي غرفة عمليات دلوقتي؟

عن طريق الـ Call Stack. ده عبارة عن "طابور" (Stack) من نوع LIFO (Last In, First Out).

1. أول ما البرنامج يبدأ، الـ Global EC بيتحط في قاع الـ Stack.
    
2. ناديت فانكشن A؟ المحرك يعمل لها EC ويحطه "فوق" الـ Global.
    
3. جوه A ناديت B؟ يعمل لـ B EC ويحطه فوق A.
    
4. خلصت B؟ يرمي الـ Context بتاعها من الـ Stack ويرجع للي تحتها (A).
    

---

  

دخلنا في "العصب الحسي" للجافا سكريبت يا خالد. النقطة الثالثة هي الـ Scope Chain (سلسلة النطاق)، ودي هي النتيجة المنطقية لوجود الـ Outer Reference جوه كل Lexical Environment.

لو الـ Execution Context هو الغرفة، والـ Lexical Environment هو القاموس، فالـ Scope Chain هو "السلم" اللي بيوصل الغرف دي ببعضها.

---

### 3. الـ Scope Chain والـ Outer Reference (سلك التوصيل)

كل Lexical Environment (سواء كان بتاع فانكشن أو بتاع Global) عنده "سلك" واصل بالبيئة اللي "احتضنته" وقت ما اتكتب. السلك ده في المحرك اسمه outer أو Outer Lexical Environment Reference.

#### أ. الميكانيكا: إزاي الـ Chain بتتبني؟

المحرك مش بيبني السلسلة دي بناءً على "مين نادى مين" (Execution)، لكن بناءً على "مين مكتوب جوه مين" (Definition/Lexical).

1. Global LE: الـ outer بتاعه بيشاور على null (لأنه مفيش حد فوقه).
    
2. Function A: لو متعرفة في الـ Global، فالـ outer بتاعها بيشاور على الـ Global LE.
    
3. Function B: لو مكتوبة جوه A (Nested)، فالـ outer بتاعها بيشاور على الـ LE بتاع A.
    

---

#### ب. الـ Identifier Resolution (رحلة البحث عن التائه)

تخيل إنك جوه الفانكشن B وطلبت متغير اسمه x. المحرك بيمشي بالترتيب ده (وده اللي بنسميه الـ Lookup Process):

1. المحطة الأولى (Local): بيبص في الـ Environment Record الحالي بتاع B. لقاه؟ خير وبركة.
    
2. المحطة الثانية (Outer Reference): ملقاش x؟ بيستخدم الـ outer عشان ينط للبيئة اللي فوقيها (A). ويبص في الـ Record بتاعها.
    
3. المحطة الثالثة (The Chain): بيفضل "يتسلق" السلسلة دي باستخدام الـ outer ريفرنس في كل بيئة، لحد ما يوصل للـ Global LE.
    
4. النهاية: لو وصل للـ Global وملقاش x هناك (والـ outer بقى null)، ساعتها المحرك يصرخ ويقولك: ReferenceError: x is not defined.
    

---

#### ج. الفرق الجوهري: Scope Chain vs. Call Stack

دي حتة بيلعبوا بيها في الإنترفيوهات يا خالد:

- الـ Call Stack: بيحدد "ترتيب التنفيذ" (مين نادى مين دلوقتي؟).
    
- الـ Scope Chain: بيحدد "الوصول للبيانات" (مين يقدر يشوف مين؟) وبناءً على مكان الكتابة مش مكان النداء.
    

مثال صايع:

لو عندك فانكشن A متعرفة في الـ Global، وناديتها من جوه فانكشن B.

الـ outer بتاع A هيفضل بيشاور على الـ Global، ومش هيشوف أي متغيرات جوه B أبداً، حتى لو B هي اللي نادته. ليه؟ لأن A "اتولدت" في الـ Global. (وده اللي بنسميه Static/Lexical Scoping).

---

#### د. الـ Shadowing (الاختباء خلف الظل)

لو عندك متغير x في الـ Local ومتغير x في الـ Global. المحرك بمجرد ما يلاقي x في الـ Local (أول محطة)، بيقف ومبيكملش تدوير في السلسلة. بنقول هنا إن الـ Local variable عمل Shadow (غطى) على الـ Global variable.

---

### 💡 ليه ده "السر الأعظم" للـ Closures؟

بما إن الفانكشن بتفضل محتفظة بالـ Outer Reference بتاعها طول حياتها، فهي بتفضل "قفشة" في البيئة اللي اتولدت فيها حتى لو الـ Execution Context بتاع البيئة دي اتمسح من الـ Stack. السلك (Outer Reference) بيفضل شغال وبيمشي فيه الكهرباء (Data)!

لو "السلم" (Scope Chain) والـ "سلك" (Outer Reference) بقوا واضحين في خيالك، قولي "كمل" عشان ندخل في النقطة الرابعة: الـ Closures (الذاكرة الممتدة) ونشوف إزاي بتستغل السلك ده عشان تعمل سحر. ⛓️

  

وصلنا لمحطة "السحر الحقيقي" في الجافا سكريبت، والنقطة الرابعة هي الـ Closures (الإغلاقات أو الذاكرة الممتدة).

إذا كان الـ Scope Chain هو "السلم" الذي يربط البيئات ببعضها، فالـ Closure هو القوة التي تمنع هذا السلم من الانهيار حتى بعد أن تُغلق الغرفة (Execution Context).

---

### 4. الـ Closures (الذاكرة الممتدة للمحرك)

الـ Closure هو ببساطة: دالة (Function) مُحاطة ببيئتها المكانية (Lexical Environment).

بشكل أدق: هو قدرة الدالة الداخلية على "تذكر" والوصول إلى المتغيرات الموجودة في النطاق المحيط بها (Parent Scope)، حتى بعد أن تنتهي الدالة الأب من التنفيذ تماماً.

#### أ. كيف يحدث السحر؟ (الميكانيكا التقنية)

عادةً، عندما تنتهي أي دالة من العمل، يحدث الآتي:

1. يتم مسح الـ Execution Context الخاص بها من الـ Call Stack.
    
2. يقوم الـ Garbage Collector (منظف الذاكرة) بمسح الـ Lexical Environment الخاص بها من الذاكرة لأنه يعتقد أنه لم يعد هناك حاجة إليه.
    

لكن في حالة الـ Closure:

إذا كانت الدالة الأب ترجع (Return) دالة داخلية، فإن هذه الدالة الداخلية تمتلك سلكاً (Outer Reference) يشير إلى الـ Lexical Environment الخاص بالأب. وطالما أن الدالة الداخلية "حية" وموجودة في كودك، فإن الـ Garbage Collector لا يمكنه مسح بيئة الأب من الذاكرة، لأن هناك من لا يزال يستخدمها!

---

#### ب. الـ Closure ليس مجرد "دالة داخلية"

هذا خطأ شائع في الإنترفيوهات. الدالة الداخلية تصبح Closure فقط عندما "تخرج" إلى العالم الخارجي وتأخذ معها حقيبة ذكرياتها (البيئة المحيطة).

مثال للتوضيح:

تخيل دالة "مصنع عدادات". الدالة الأب تنتهي وتختفي، لكن العداد الذي صنعته يظل يتذكر قيمة المتغير count ويقوم بزيادته في كل مرة. الـ count هنا ليس مخزناً في الدالة الأب (لأنها ماتت)، بل هو "محبوس" داخل الـ Lexical Environment الذي تحتفظ به الدالة الـ Closure.

---

#### ج. لماذا نحتاج الـ Closures؟ (القيمة الهندسية)

1. البيانات الخاصة (Data Privacy): هي الطريقة الوحيدة في الجافا سكريبت (قبل ظهور الـ Private class fields) لعمل متغيرات لا يمكن لأحد الوصول إليها من الخارج، فقط الدالة الـ Closure هي من تملك المفتاح.
    
2. تخزين الحالة (State Management): تسمح للدوال بأن يكون لها "ذاكرة" خاصة بها دون الحاجة لاستخدام متغيرات Global تلوث الكود.
    
3. الـ Function Factories: تمكنك من إنشاء دوال متخصصة بناءً على بارامترات معينة (مثل دوال تحويل العملات أو حساب الضرائب).
    

---

#### د. الثمن: تسريب الذاكرة (Memory Leaks)

لأن الـ Closures تمنع الـ Garbage Collector من مسح البيئات القديمة، فإساءة استخدامها أو عمل آلاف الـ Closures دون داعٍ قد يؤدي إلى استهلاك كبير للميموري. المحرك يظل محتفظاً بالبيانات "فقط في حالة" احتاجتها الدالة الـ Closure لاحقاً.

---

وصلنا للمحطة الخامسة، وهي "دستور" البرمجة الوظيفية (Functional Programming). النقطة الخامسة هي الـ Pure Functions (الدوال النقية) والـ Side Effects (الآثار الجانبية).

لو الـ Execution Context هو الغرفة، فالـ Pure Function هي الآلة اللي جوه الغرفة دي، اللي بتشتغل بنظام رياضي صارم؛ مدخلات محددة بتؤدي لنتائج حتمية بدون ما "توسخ" المكان حواليها.

---

### 5. الـ Pure Functions (المعادلة الرياضية المتزنة)

في الجافا سكريبت، مش كل الفانكشنز "نقية". الفانكشن بتعتبر Pure فقط لو حققت "قانونين" ملهومش تالت:

#### أ. القانون الأول: الحتمية (Determinism)

الفانكشن لازم ترجع نفس النتيجة لو خدت نفس المدخلات، مهما ناديتها (النهارده، بكره، مليون مرة).

- ليه ده عميق؟ لأن الفانكشن دي مش بتعتمد على أي حاجة "بره" صندوقها (زي متغير Global أو الساعة أو قيمة عشوائية). هي "مكتفية ذاتياً".
    

#### ب. القانون الثاني: انعدام الآثار الجانبية (No Side Effects)

الفانكشن "النقية" مش بتغير أي حاجة في "العالم الخارجي". هي بتاخد داتا، بتعمل حسبة، وبترجع داتا جديدة.

- يعني إيه Side Effect أصلاً؟ هو أي تغيير بيحصل بره حدود الفانكشن. زي:
    

- تعديل متغير Global.
    
- الطباعة في الـ Console (console.log).
    
- الكتابة في ملف أو Database.
    
- حتى نداء فانكشن تانية "مش نقية".
    

---

### 🧐 ليه الـ Side Effects بتعتبر "ضوضاء"؟

تخيل إنك بتبني مفاعل نووي (Software معقد). لو كل فانكشن بتغير في متغيرات بره، فجأة هتلاقي قيم بتتغير وأنت مش عارف مين اللي غيرها! ده اللي بيسموه الـ Spaghetti Code.

الـ Side Effects بتخلي الكود صعب توقعه (Unpredictable) وصعب جداً في الـ Testing، لأنك لازم تجهز "البيئة الخارجية" كلها عشان تعرف تختبر فانكشن واحدة.

---

### 🏗️ الميكانيكا العميقة: إزاي بنهندل ده في الواقع؟

أنت كمهندس، أكيد بتسأل: "طيب لو مفيش Side Effects، السيرفر هيكلم الـ DB إزاي؟ أو هيطبع Error إزاي؟"

السر هنا مش في منع الـ Side Effects، بل في "عزلها" (Isolation):

1. الفصل الجراحي: بنخلي الـ Logic الصعب (الحسابات، الفلترة، التحويلات) كله في Pure Functions. دي بتبقى سهلة جداً في الـ Testing لأنها مش محتاجة DB ولا سيرفر.
    
2. حاويات الـ Side Effects: بنحط الـ Side Effects في أماكن محددة وواضحة (زي الـ Controllers في نود)، وبنسميها Impure shell. كأننا بنقول: "الوساخة كلها هتبقى في الأوضة دي بس، وباقي البيت نظيف".
    

---

### 🌟 فوايد الـ Pure Functions:

1. الـ Predictability: الكود بيبقى "كتاب مفتوح". أنت عارف إن add(2, 2) هتفضل 4 للأبد.
    
2. الـ Testability: مش محتاج تعمل "Mocking" للـ Database عشان تختبر Logic حسابي. بتبعت input وبتاخد output وخلاص.
    
3. الـ Parallelism (التوازي): لأن الفانكشن مش بتغير في داتا بره، فإحنا نقدر نشغل 100 نسخة منها في نفس الوقت من غير ما يخبطوا في بعض (No Race Conditions).
    
4. الـ Memoization: بما إن النتيجة ثابتة للمدخلات الثابتة، فإحنا نقدر "نكشّ" (Cache) النتيجة ونوفر وقت الحساب في المرات الجاية.
    

---

وصلنا للمحطة السادسة في "دستور" البرمجة الوظيفية، وهي الحارس الشخصي للبيانات. النقطة السادسة هي الـ Immutability (عدم قابلية التغيير).

لو الـ Pure Function هي المحرك اللي بيشتغل بنظافة، فالـ Immutability هي "المادة الخام" اللي مستحيل تتشوه أو تتغير بالصدفة أثناء الشغل.

---

### 6. الـ Immutability (الداتا المقدسة)

في البرمجة التقليدية (Imperative)، إحنا متعودين نغير في الداتا طول الوقت (نعدل Variable، نزود رقم في Array، نغير خاصية في Object). في الـ Functional Programming، ده "محرم".

المعنى: بمجرد ما الـ Data Structure (Object أو Array) تتخلق، ممنوع تعدلها. لو محتاج تغير حاجة، متعدلش في الأصل؛ خد نسخة وعدل فيها.

#### أ. التشبيه الفيزيائي (السبورة ضد الكراسة)

- الـ Mutable (قابل للتغيير): زي "السبورة". بتكتب عليها، وبعدين تمسح وتعدل في نفس المكان. المشكلة؟ لو فيه 3 مهندسين شغالين على نفس السبورة، واحد ممكن يمسح شغل التاني بالغلط (Race Condition).
    
- الـ Immutable (غير قابل للتغيير): زي "الكراسة". لو كتبت صفحة وعاوز تعدل، مش بتمسح؛ أنت بتقلب الصفحة وتكتب النسخة الجديدة المعدلة. الصفحة القديمة بتفضل زي ما هي "مرجع" ليك.
    

---

#### ب. الميكانيكا العميق: ليه الـ Objects "خاينة" في الجافا سكريبت؟

أنت عارف إن الـ Primitives (رقم، string) في JS هما immutable بطبعهم. لكن الـ Objects والـ Arrays بيشتغلوا بالـ Reference.

> [!CAUTION] reference trap 
> لو عندك const user = { name: "Khaled" } وقمت عامل const admin = user.
   لو غيرت admin.name = "Mohamed"، الـ user.name هو كمان هيتغير!
   ليه؟ لأن الاتنين بيشاوروا على نفس المكان في الميموري. دي كارثة في الأنظمة المعقدة.





---

#### ج. إزاي بنطبق الـ Immutability بجد؟ (The Modern Way)

بدل ما نستخدم Methods بتغير الأصل (زي push, splice, sort)، بنستخدم الـ Spread Operator والـ Non-mutating methods:

1. تعديل Object:
    
2. JavaScript
    

const user = { id: 1, name: "Khaled" };

// ✅ الصح: نسخة جديدة

const updatedUser = { ...user, name: "M. Khaled" }; 

3.   
    
4.   
    
5. إضافة لـ Array:
    
6. JavaScript
    

const list = [1, 2, 3];

// ✅ الصح: Array جديدة

const newList = [...list, 4]; 

7.   
    
8.   
    
9. حذف من Array:  
    بنستخدم الـ filter لأنها بترجع Array جديدة تماماً.
    

---

#### د. فوائد الـ 

1. الـ Predictability: مفيش فانكشن في "آخر الشارع" هتغير لك متغير أنت بتستخدمه "أول الشارع" من غير ما تعرف.
    
2. الـ Time Travel Debugging: بما إن كل "نسخة" من الداتا لسه موجودة في الميموري (لو محتفظ بـ reference ليها)، تقدر ترجع لحالة البرنامج في أي لحظة سابقة بسهولة (زي الـ Redux DevTools).
    
3. سهولة المقارنة (Change Detection): المحرك مش محتاج يلف جوه الـ Object سطر بسطر عشان يعرف لو فيه حاجة اتغيرت؛ هو بس بيقارن الـ Memory Reference. لو الـ Reference اختلف، يبقى الداتا اتغيرت. ده اللي بيخلي React سريعة جداً.
    

---

### 💡 زتونة الـ Shadow Interview:

"الـ Immutability هي المبدأ اللي بيمنع الـ Side Effects الناتجة عن تعديل الـ State بشكل مباشر. بدلاً من الـ Mutation، نقوم دائماً بإنشاء New Copies من البيانات. هذا يضمن أن البيانات تظل Predictable ويجعل تتبع الـ Bugs أسهل بكثير لأن البيانات لا تتغير من خلف ظهورنا."

---

  بما أننا انتهينا من المبادئ الأساسية للبرمجة الوظيفية (Functional Programming)، سننتقل الآن إلى "الميكانيكا الدقيقة" لكيفية تعامل محرك الجافا سكريبت مع أنواع مختلفة من سياقات التنفيذ (Execution Contexts) وكيفية تأثيرها على مفاهيم مثل `this` والـ Currying.

سنبدأ بالنقطة السابعة وهي العلاقة الشائكة بين **Arrow Functions** وكلمة **`this`**.

---

### 7. الـ Arrow Functions والـ Lexical `this`

تعتبر الـ Arrow Functions (الدوال السهمية) التي قُدمت في ES6 أكثر من مجرد "اختصار" في الكتابة؛ فهي تمتلك ميكانيكا داخلية مختلفة تماماً عن الدوال العادية (Regular Functions) فيما يخص سياق التنفيذ.

#### أ. غياب الـ `this` الذاتي (No Own `this`)
في الدالة العادية، عندما يتم إنشاء **Execution Context** لها، يقوم المحرك تلقائياً بإنشاء "رابط" (Binding) لكلمة `this` بناءً على **كيفية استدعاء الدالة** (Dynamic Binding).
أما في الـ Arrow Function، فإن الـ **Environment Record** الخاص بها **لا يحتوي** على مكان لتخزين قيمة `this`. 



#### ب. الميكانيكا اللكسيكالية (Lexical Look-up)
لأن الـ Arrow Function لا تملك `this` خاصة بها، فإنها تعامل كلمة `this` كأي متغير عادي آخر. عندما تطلب `this` داخلها، يقوم المحرك بالبحث عنها في الـ **Lexical Environment** المحيط بها (Parent Scope) عبر الـ **Outer Reference**.
* **النتيجة:** قيمة `this` داخل الـ Arrow Function تتحدد بناءً على **المكان الذي عُرّفت فيه** (Definition-time)، وليس المكان الذي استُدعيت منه (Run-time).

#### ج. غياب الـ `arguments` والـ `super`
بنفس المنطق، الـ Arrow Functions لا تملك كائن `arguments` خاصاً بها ولا تملك `super`. هي دوال "خفيفة" (Lightweight) صُممت لتكون جزءاً من السياق الذي تعيش فيه، لا أن تخلق سياقاً مستقلاً تماماً.

---

### 8. الـ Eval Execution Context (سياق التنفيذ الدخيل)

الجافا سكريبت تمتلك نوعاً ثالثاً من سياقات التنفيذ يسمى **Eval Execution Context**. هذا السياق يُنشأ عند استخدام دالة `eval()`.

#### أ. كيف يعمل؟
عند تنفيذ `eval("console.log(x)")` برمجياً، يقوم المحرك بإنشاء EC خاص لهذا الكود. الميزة (أو العيب) هنا أن هذا السياق يمتلك وصولاً كاملاً للـ **Lexical Environment** الخاص بالدالة التي استدعته.
* يمكن لـ `eval` تعديل المتغيرات في النطاق المحيط بها، مما يجعل الكود صعب التوقع.

#### ب. لماذا يُنصح بتجنبه؟
المحركات الحديثة (مثل V8) تقوم بعمل تحسينات (Optimizations) بناءً على تحليل الـ **Lexical Environment** مسبقاً. استخدام `eval` يكسر هذه التحسينات لأن المحرك لا يمكنه معرفة ما سيفعله الكود المكتوب كنص (String) إلا في لحظة التنفيذ، مما يؤدي لبطء شديد ومخاطر أمنية.

---

### 9. الـ Currying Functions (تجزئة الدوال)

الـ **Currying** هو مفهوم رياضي (منطق هاسكل كاري) يتم تطبيقه في البرمجة الوظيفية لتحويل دالة تأخذ عدة مدخلات إلى سلسلة من الدوال، كل واحدة منها تأخذ مدخلاً واحداً فقط.

#### أ. الميكانيكا والربط بالـ Closures
الـ Currying يعتمد كلياً على الـ **Closures** والـ **Lexical Environment**. 
عندما تنادي `f(a)(b)(c)`:
1. الدالة الأولى تأخذ `a` وتخزنه في الـ LE الخاص بها وترجع دالة ثانية.
2. الدالة الثانية "تقفل" (Closure) على قيمة `a` وتنتظر `b`.
3. الدالة الثالثة تصل لكل من `a` و `b` عبر الـ **Scope Chain** لتخرج النتيجة النهائية.



#### ب. الفائدة الهندسية
* **Partial Application:** يمكنك تثبيت بعض المدخلات (مثل إعدادات عامة) وإنشاء دوال متخصصة منها.
* **التركيب (Composition):** يسهل دمج الدوال مع بعضها كأنها قطع ليغو.

---
وصلنا للمحطة الثامنة، وهي "الغرفة المشبوهة" في محرك الجافا سكريبت. النقطة الثامنة هي **الـ `eval` Execution Context (سياق تنفيذ التقييم)**.

لو الـ Global EC هو الصالة الكبيرة، والـ Function EC هي الغرف الخاصة، فالـ **`eval`** هو "خيمة" بتتبني فجأة جوه أي أوضة تانية، وبتقدر تغير في أثاث الأوضة دي وهي واقفة!

---

### 8. الـ `eval` Execution Context (الخيمة الدخيلة)

الدالة `eval()` هي دالة برمجية بتاخد "نص" (String) وبتنفذه كأنه كود جافا سكريبت حقيقي. لما بتنادي `eval()`، المحرك بيعمل لها **Execution Context** خاص بيها، بس فيه شوية "عفرتة" بتحصل.

#### أ. كيف يختلف الـ `eval` عن إخوته؟

1. **التبعية المكانية:** الـ `eval` EC ملوش مكان ثابت؛ هو بيستخدم الـ **Lexical Environment** بتاع المكان اللي اتنادى فيه.
    
2. **تعديل البيئة (In-place Mutation):** وده أخطر جزء. الـ `eval` يقدر يضيف متغيرات جديدة للـ Environment Record بتاع الفانكشن اللي هو جواها، وده بيبوظ مبدأ الـ **Lexical Scope** الثابت.
    

---

#### ب. ليه السينيورز بيقولوا "eval is evil"؟ (الجانب المظلم)

فيه سببين بيخلوا أي مبرمج محترف يهرب من الـ `eval`:

**١. ضياع الأداء (Performance Loss):**

محرك الـ V8 ذكي جداً؛ هو بيعمل "Optimizations" للكود بتاعك قبل ما يشغله لأنه عارف أماكن كل المتغيرات (عارف الخريطة اللكسيكالية). لما بتستخدم `eval` بقيمة متغيرة، المحرك "بيصدم"؛ لأنه مش عارف الـ `eval` ده هيضيف إيه أو هيمسح إيه. فبيضطر يوقف كل الـ Optimizations ويشغل الكود ببطء شديد عشان يضمن الدقة.

**٢. الثغرات الأمنية (Security Risks):**

لو الـ `eval` دي بتاخد داتا جاية من يوزر (زي Input field)، اليوزر ده يقدر يبعت كود خبيث يسرق الـ Cookies أو يمسح داتا من السيرفر. أنت كأنك بتدي "ميكروفون" لأي حد معدي في الشارع يزعق بيه جوه قلب السيستم بتاعك.

---

#### ج. الـ Strict Mode وحل الأزمة

في الـ **Strict Mode** (`'use strict'`), الجافا سكريبت حاولت تهذب الـ `eval`. لما بتشغله في الـ Strict mode، هو بيعمل لنفسه **Lexical Environment** منفصل تماماً؛ يعني لو عرفت متغير جوه الـ `eval` مش هيظهر بره في الفانكشن المحيطة. وده خلى استخدامه أأمن شوية وأسرع سيكا، بس لسه مكروه.

---

#### د. الـ `eval` vs الـ `new Function()`

فيه بديل للـ `eval` وهو `new Function('code')`. الفرق الجوهري إن `new Function` بتعمل فانكشن "مقطوعة الصلة" بالمكان اللي اتكتبت فيه؛ الـ **Outer Reference** بتاعها دايماً بيشاور على الـ **Global LE**، فمش بتقدر "تحشر" نفسها وتعدل في المتغيرات الـ Local بتاعة الفانكشن اللي نادتها.

---

### 💡 زتونة الـ Shadow Interview:

> "الـ **`eval` Execution Context** هو نوع تالت من البيئات بيتم إنشاؤه وقت التنفيذ (Runtime) لتقييم كود نصي. خطورته بتكمن في إنه بيكسر الـ **Lexical Scope** التقليدي وبيمنع المحرك من عمل **Optimizations**، وده بيأثر بالسلب على الـ Performance والأمن. القاعدة الذهبية: 'Never use eval unless you are building a compiler or a very specific dev tool'."

---

وصلنا للمحطة الأخيرة في رحلتنا، وهي "كريمة" البرمجة الوظيفية. النقطة التاسعة هي **الـ Currying Functions (تجزئة الدوال)**.

لو الـ Closure هو "الذاكرة"، فالـ **Currying** هو الفن اللي بيخلينا نستخدم الذاكرة دي عشان نبني كود "مرن" زي المكعبات، بنركبه حتة حتة حسب الحاجة.

---

### 9. الـ Currying Functions (التقسيط البرمجي)

الـ **Currying** هو تقنية في البرمجة الوظيفية بتحول فانكشن بتاخد كذا Parameter (مثلاً `a, b, c`) لسلسلة من الفانكشنز، كل واحدة فيهم بتاخد **Parameter واحد بس** وترجع فانكشن تانية مستنية الـ Parameter اللي بعده.

يعني بدل ما تنادي: `f(a, b, c)`

بتنادي: `f(a)(b)(c)`

#### أ. الميكانيكا العميقة: إزاي الـ Closures بتشغل الـ Currying؟

الـ Currying مستحيل يشتغل من غير الـ **Closures** والـ **Lexical Environment** اللي "فرتكناهم" فوق.

1. لما بتنادي `f(a)`، المحرك بيعمل **Execution Context** وببيخزن `a` في الـ **Environment Record**.
    
2. الفانكشن بترجع فانكشن تانية (Internal Function).
    
3. الفانكشن التانية دي "قفشة" في قيمة `a` عن طريق الـ **Closure**.
    
4. لما تنادي `(b)`، الفانكشن الجديدة بتعمل نفس الكلام وتفضل "قفشة" في `a` و `b` لحد ما توصل لآخر Parameter وتنفذ الـ Logic النهائي.
    

---

#### ب. ليه الـ Currying "عبقري"؟ (Partial Application)

أهم ميزة هي الـ **Partial Application** (التطبيق الجزئي). أنت بتقدر "تثبت" جزء من المدخلات وتعمل فانكشن جديدة متخصصة.

**مثال للتوضيح:**

تخيل عندك فانكشن بتحسب السعر بعد الضريبة: `calculateTax(taxRate, price)`.

باستخدام الـ Currying، تقدر تعمل:

JavaScript

```
const calculateTax = rate => price => price + (price * rate);

// الآن نقدر نصنع فانكشنز متخصصة
const egyptTax = calculateTax(0.14); // ثبتنا الـ 14%
const vatTax = calculateTax(0.05);   // ثبتنا الـ 5%

// نستخدمهم بعدين براحتنا
console.log(egyptTax(100)); // 114
console.log(vatTax(100));   // 105
```

هنا الـ `egyptTax` هي فانكشن "محملة بذكريات" الـ `rate` بفضل الـ **Lexical Environment** بتاعها.

---

#### ج. الربط بالـ Higher-Order Functions

الـ Currying هو تطبيق عملي بحت للـ **HOF**. أنت بتبني فانكشن بتنتج فانكشنز تانية. ده بيخلي كودك **Declarative** جداً؛ أنت مش بتقول للكمبيوتر "اضرب السعر في كذا"، أنت بتقوله "استخدم فانكشن ضرائب مصر على المنتج ده".

---

#### د. الفرق بين الـ Currying والـ Partial Application

عشان لو سألك فيها سينيور "رخم":

- **Currying:** هو تحويل الفانكشن لـ سلسلة من الفانكشنز كل واحدة بتاخد **1** parameter بالظبط.
    
- **Partial Application:** هو إنك تثبت عدد **أي كان** من الـ parameters وتنتج فانكشن مستنية الباقي (ممكن تاخد 2 أو 3 مرة واحدة).
    

---

### 💡 زتونة الـ Shadow Interview والخلاصة النهائية:

> "الـ **Currying** هو تحويل دالة متعددة الوسائط إلى سلسلة من الدوال أحادية الوسيط. يعتمد كلياً على الـ **Closures** للحفاظ على حالة الوسائط السابقة. فائدته العظمى تكمن في إعادة استخدام الكود (Code Reusability) وخلق دوال متخصصة من دوال عامة، مما يجعل الكود أكثر **Modular** وسهل الاختبار."

---

  
  
